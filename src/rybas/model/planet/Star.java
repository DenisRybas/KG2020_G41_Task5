package rybas.model.planet;

import rybas.math.Vector2;

public class Star extends SpaceObject{
    public Star(double mass, double radius, Vector2 position,
                double density) {
        super(mass, radius, position, density);
    }
}
